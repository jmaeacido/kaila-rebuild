"use client";
import { useEffect, useState } from "react";import { Check, RefreshCw, ShieldCheck, Tags, X } from "lucide-react";import styles from "./page.module.css";
type Provider={id:number;display_name:string;bio:string};type Credential={id:number;label:string;type:string};type Asset={id:string;original_name:string;mime_type:string};
export default function AdminHome(){const[providers,setProviders]=useState<Provider[]>([]);const[credentials,setCredentials]=useState<Credential[]>([]);const[assets,setAssets]=useState<Asset[]>([]);const[state,setState]=useState<"loading"|"ready"|"error">("loading");function apply(body:{data:{providers:Provider[];credentials:Credential[];assets:Asset[]}}){setProviders(body.data.providers);setCredentials(body.data.credentials);setAssets(body.data.assets);setState("ready")}async function load(){setState("loading");try{const r=await fetch("/api/v1/admin/marketplace/review-queue",{credentials:"include"});if(!r.ok)throw new Error();apply(await r.json()as{data:{providers:Provider[];credentials:Credential[];assets:Asset[]}})}catch{setState("error")}}useEffect(()=>{void fetch("/api/v1/admin/marketplace/review-queue",{credentials:"include"}).then(async r=>{if(!r.ok)throw new Error();apply(await r.json()as{data:{providers:Provider[];credentials:Credential[];assets:Asset[]}})}).catch(()=>setState("error"))},[]);async function review(kind:"providers"|"credentials"|"assets",id:number|string,approve:boolean){await fetch("/api/v1/auth/csrf",{credentials:"include"});const token=document.cookie.split("; ").find(x=>x.startsWith("XSRF-TOKEN="))?.split("=")[1];const url=kind==="providers"?`/api/v1/admin/marketplace/providers/${id}/status`:kind==="credentials"?`/api/v1/admin/marketplace/credentials/${id}/review`:`/api/v1/admin/marketplace/assets/${id}/scan`;const body=kind==="providers"?{status:approve?"active":"rejected"}:kind==="credentials"?{reviewStatus:approve?"approved":"rejected"}:{scanStatus:approve?"clean":"rejected"};await fetch(url,{method:"PUT",credentials:"include",headers:{"Content-Type":"application/json",...(token?{"X-XSRF-TOKEN":decodeURIComponent(token)}:{})},body:JSON.stringify(body)});await load()}return <main className={styles.page}><header><div><p>KAILA ADMINISTRATION</p><h1>Marketplace review</h1></div><button onClick={()=>void load()}><RefreshCw/>Refresh</button></header>{state==="error"&&<div className={styles.error}>The review queue could not be loaded.</div>}{state==="loading"&&<div className={styles.loading}>Loading review queue…</div>}{state==="ready"&&<div className={styles.columns}><Queue title="Safety scans" icon={<ShieldCheck/>} empty="No files need scan review.">{assets.map(x=><article key={x.id}><h3>{x.original_name}</h3><p>{x.mime_type}</p><Actions onChoose={v=>void review("assets",x.id,v)}/></article>)}</Queue><Queue title="Provider profiles" icon={<ShieldCheck/>} empty="No provider profiles need review.">{providers.map(x=><article key={x.id}><h3>{x.display_name}</h3><p>{x.bio}</p><Actions onChoose={v=>void review("providers",x.id,v)}/></article>)}</Queue><Queue title="Credentials" icon={<Tags/>} empty="No credentials need review.">{credentials.map(x=><article key={x.id}><h3>{x.label}</h3><p>{x.type}</p><Actions onChoose={v=>void review("credentials",x.id,v)}/></article>)}</Queue></div>}</main>}function Queue({title,icon,empty,children}:{title:string;icon:React.ReactNode;empty:string;children:React.ReactNode}){return <section><h2>{icon}{title}</h2>{Array.isArray(children)&&children.length===0?<p className={styles.empty}>{empty}</p>:children}</section>}function Actions({onChoose}:{onChoose:(v:boolean)=>void}){return <div className={styles.actions}><button className={styles.approve} onClick={()=>onChoose(true)}><Check/>Approve</button><button className={styles.reject} onClick={()=>onChoose(false)}><X/>Reject</button></div>}
